export function HyperClass(componente, style) {
    // Aplicar el estilo al componente
    componente.style.cssText = style;
  
    // Devolver el componente con el estilo aplicado
    return componente;
  }
  